name: flutter_clean_architecture_task
description: "This project is for hiring progress of AbanTether company written by AmirAbbas Jannatian"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  #State management
  flutter_bloc: ^9.0.0

  #Dependency injection
  get_it: ^8.0.3

  #Local storage
  encrypt_shared_preferences: ^0.8.4

  #For comparing data objects
  equatable: ^2.0.7
  dio: ^5.8.0+1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  test: ^1.25.8

flutter:
  uses-material-design: true